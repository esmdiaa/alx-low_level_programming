#include <stdio.h>
/**
 * main - A typing program that prints a line using printf
 * return: 0 (Success)
 */

int main (void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
